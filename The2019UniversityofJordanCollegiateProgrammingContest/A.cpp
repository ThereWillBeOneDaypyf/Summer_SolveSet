#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int a,b;
    while(cin >> a >> b)
    {
        if(a >= b)
            cout << 1 << endl;
        else cout << 0 << endl;
    }
    return 0;
}